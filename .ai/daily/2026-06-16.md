# Daily Log: 2026-06-16

## Starting State

- Branch: `ui-update`.
- Existing local dirty state includes deleted `package-lock.json`, modified `Card.tsx` and `Sidebar.tsx`, and local agent database artifacts.
- `npm run lint` passed before implementation.
- `npm run build` passed before implementation.
- `pnpm audit --prod` reported vulnerable dependency surface that will be addressed by dependency cleanup and upgrades.

## Plan

- Add governance records first.
- Standardize pnpm/Node.
- Retire private legacy features.
- Rebuild public portfolio and verification gates.

## Checkpoints

- Added `/Users/ogiboy/Documents/Projects/portfolio-app` to Codex trusted projects in `/Users/ogiboy/.codex/config.toml` so project-local config, hooks, and exec policies can load in future sessions.
- Pushed an early checkpoint on `ui-update` to reduce VSCode/Git load after the worktree started tracking many overhaul changes.
- Rebuilt missing site/client components after accidental local revert mixed the post-checkpoint edits.
- Added project route smoke tests, content invariants, CI workflow, changelog, and README release gates.
- Added explicit Prettier scope and Tailwind-aware formatting plan.
- Added home-media-portal WASM game center as a live-demo roadmap item with backend expansion deferred to a separate decision.
- Fixed Next dev startup by installing `babel-plugin-react-compiler` for the enabled React Compiler path and pinning `turbopack.root`.
- Switched the production build gate to `next build --webpack` because Turbopack production build stalled on Next.js 16.2.9, while Webpack completed cleanly.
- Resolved the PostCSS audit finding by forcing `postcss@8.5.15` through `pnpm-workspace.yaml` overrides.
- Reworked Playwright smoke tests to start the local portfolio app and removed scaffold tests that depended on external Playwright documentation.
- Implemented the first WASM game center live demo at `/[locale]/labs/retro-game-center` with lazy iframe boot, copied static engine/ROM assets, and a narrow `/wasm/*` route handler.
- Fixed remote CI typecheck drift by adding tracked static image module declarations instead of relying on ignored `next-env.d.ts`.
- Remote GitHub Actions passed for `6ac60d99` on run `27646525569`.
- Browser QA covered `/en`, `/tr`, `/en/projects`, `/tr/projects`, and `/en/labs/retro-game-center` on local Turbopack dev.
- Tightened the WASM iframe sandbox by removing `allow-same-origin` and redundant `allowFullScreen`; follow-up QA showed no browser console warnings.
- Added the v0.2.0 release checklist under `docs/aegis/release/` with gate, CI, and browser QA evidence.
- Vercel preview deploy failed because the project defaulted to pnpm 9 despite repo `engines.pnpm >=11.7.0`; added `vercel.json` install/build overrides to activate pnpm 11.7.0 through corepack.
