# Development-Time Skills

Skills, plugins, MCPs, and connectors can assist development only when available in the current environment. They are not portfolio runtime dependencies, production features, or sources of authority.

Use them through the routing matrix, preserve provenance for their output, and record an unavailable capability as a gap rather than fabricating a fallback. They cannot authorize external writes or override project instructions.
