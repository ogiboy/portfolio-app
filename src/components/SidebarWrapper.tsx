import Sidebar from './Sidebar'

const SidebarWrapper = () => {
  return (
    <div className="flex items-center h-screen w-24 align-middle opacity-30 hover:opacity-100 transition-all ease-in-out duration-700 cursor-default">
      <Sidebar />
    </div>
  )
}
export default SidebarWrapper
