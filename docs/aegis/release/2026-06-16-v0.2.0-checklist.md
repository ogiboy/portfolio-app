# Release Checklist: v0.2.0 Portfolio Overhaul

## Scope

- Public EN/TR portfolio rebuild on `ui-update`.
- Brutalist Dev Lab design system with shadcn/Radix primitives.
- App Router Server Components by default with client behavior pushed to leaf components.
- Isolated GSAP motion and WASM game center lab route.
- No public Auth0 dashboard, Postgres guestbook, or private database surface in v0.2.0.

## Verification Evidence

- Vercel Node compatibility: `.node-version` stays `24.16.0` for local/CI, while `engines.node` allows Vercel Node 24.x images from `24.15.0`.
- Vercel install/build override: `vercel.json` runs `corepack pnpm@11.7.0 install --frozen-lockfile` and `corepack pnpm@11.7.0 build` directly.
- `pnpm format:check`: passed on 2026-06-16.
- `pnpm lint`: passed on 2026-06-16.
- `pnpm typecheck`: passed on 2026-06-16.
- `pnpm test`: 2 files and 8 tests passed on 2026-06-16.
- `pnpm test:e2e`: 1 Chromium smoke test passed on 2026-06-16.
- `pnpm build`: `next build --webpack` completed 46/46 static pages on 2026-06-16.
- `pnpm audit --prod --audit-level high`: no known vulnerabilities found on 2026-06-16.
- Remote GitHub Actions: `ui-update` run `27646525569` for commit `6ac60d99` completed successfully.
- Browser QA: `/en`, `/tr`, `/en/projects`, `/tr/projects`, and `/en/labs/retro-game-center` rendered successfully in Chromium on local Turbopack dev.
- WASM QA: iframe count is zero before boot; boot action creates `/wasm/engine/index.html?game=doom-shareware`; sandbox is `allow-scripts allow-pointer-lock allow-downloads`; browser console errors and warnings are empty.

## Known Follow-Ups

- Validate the Vercel preview deployment after the `vercel.json` corepack/pnpm override is pushed.
- Self-host the DOSBox engine frame's CDN vendor scripts when package/vendor review is scheduled.
- Revisit `pnpm build:turbopack` after the current Next.js 16 Turbopack production build stall is resolved upstream.
